export enum HomePageProductsLocator {
  defaultCheckoutButton = '[class="btn btn-default check_out"]',
  registryCheckoutButton = '[class="modal-body"] a',
  productImageWrapper = '.product-image-wrapper',
  shopping_cart = '.add-to-cart',
}